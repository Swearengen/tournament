// import * as _ from 'lodash'

// export function toPairs<T>(array: T[]) {
//     const evens = array.filter((o, i) => i % 2);
//     const odds = array.filter((o, i) => !(i % 2));
//     return _.zipWith(evens, odds, (e, o) => e ? [o, e] : [o]);
// }